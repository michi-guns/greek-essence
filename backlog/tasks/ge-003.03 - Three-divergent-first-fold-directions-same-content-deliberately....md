---
id: GE-003.03
title: 'Three divergent first-fold directions - same content, deliberately...'
status: In Progress
assignee:
  - '@claude'
created_date: '2026-09-21 19:14'
updated_date: '2026-09-23 18:54'
labels:
  - 'lane:product'
  - 'phase:P1.1'
milestone: m-1
dependencies: []
parent_task_id: GE-003
type: feature
ordinal: 25000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Planning ref: T-01.3 (docs/v1/04-ROADMAP.md)
est: 150/270/540 min (min/avg/max)
Phase P1.1 - Product lane

Three divergent first-fold directions - same content, deliberately different (type-led / image-led / editorial-grid), built as real code, screenshotted side by side
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Three direction routes render the same content (header, headline, lede, three sample packages, one action): /directions/type-led, /directions/image-led, /directions/editorial-grid, each following its 02-DESIGN-SYSTEM §4.6 column
- [ ] #2 Every direction passes the §4.8 measurable checks: display weight <= 500, body 60-72 characters per line, radius 0 on images and buttons, one solid button per viewport; saturated pixels and bare ground reported per direction
- [ ] #3 Quality floor at 320, 390, 768, 1024 and 1440: no horizontal scroll, no two-line buttons, no overflowing headline; visible teal focus ring; reduced-motion shows everything at rest
- [ ] #4 Placeholder facts (package names, day counts, photos) are plainly labelled as placeholders on the page, and every photo is logged with source URL, author and licence (D-034)
- [ ] #5 Direction routes cannot reach production: noindex, 404 when Netlify CONTEXT is production, and a removal criterion is added to GE-005.01
- [ ] #6 A local side-by-side report in .local/design-exploration/2026-09-23-first-fold-directions/ shows each direction at 390 and 1440 (fold and full page) with its name, idea, signature moment, risk, the §4.8 numbers and one recommendation
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 Acceptance criteria met; final summary records what was checked and what remains
- [ ] #2 typecheck, lint and unit tests pass
- [ ] #3 User-visible change: live walk completed per A-007 with evidence in .local/evidence/ - or state why not applicable
- [ ] #4 Anything odd noticed is filed as a task, not dropped
- [ ] #5 Task branch merged into its phase branch
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Where it lives: app/directions/ - index page, three route folders (type-led, image-led, editorial-grid), _shared/ (content.ts with the one content set and photo credit log, demo-bar.tsx as a Server Component, directions.css scoped per direction by a root class). No files in components/ (throwaway, one caller each; DS-002). Removed by GE-005.01: add an AC there (backlog task edit) naming app/directions/ and tests/e2e/directions.spec.ts.
2. Guard: app/directions/layout.tsx sets robots noindex and calls notFound() when process.env.CONTEXT === 'production'. Verified in its failing state: CONTEXT=production pnpm build, then /directions and /directions/type-led return 404 + noindex; build output lists the routes as static.
3. Fonts: directions layout imports @fontsource-variable/fraunces/full.css and full-italic.css (opsz, SOFT, true italic). directions.css resets the global h1-h3 rules (weight 600, max-width 12ch), sets font-synthesis: none so a missing italic shows as upright rather than fake, display weight 300-400, radius 0, no card border/fill/shadow. Walk confirms the true italic renders.
4. Photos: 8 free Unsplash photos (not Unsplash+), bare images.unsplash.com URLs, through next/image and the existing loader. Architecture, texture, food, landscape; no identifiable people, no private hotel property. Logged in content.ts (author, page URL, Unsplash License) and in the report.
5. Content (same in all three): header (Menu, wordmark, Plan your trip), roman+italic headline, serif lede, three placeholder packages (place, dek, region, days), text links. No prices (D-006). Labelling: a fixed demo bar on every direction page says copy, packages, day counts and photos are placeholders.
6. Build per 02 §4.6, DS-004 clamp sizes interpolated 390->1440 (display clamp(2.5rem, 1.571rem + 3.81vw, 5rem); max <= 2.5x min), DS-007 ratios (inset hero 3:2, cards 4:5, full-bleed hero 4:5 below md, 3:2 md-lg, min(100svh - header, 56rem) from lg written literally). One solid button per viewport: type-led and editorial grid - header Plan your trip is the solid button, everything else text links; image-led - Plan your trip is an outline button on the photograph, header sits over the hero and scrolls away (the DS-006 colour change needs motion.js), and the one solid button is the closing Plan your trip after the cluster. Motion per DS-006 in CSS, inside @media (prefers-reduced-motion: no-preference): hover colour/underline 220ms, card image 1.03 over 700ms, hero settle 1.04->1 over 1600ms, italic line fade-up 800ms starting 150ms after. Scroll reveal left out (public/motion.js, GE-004.05) - stated in the report.
7. Screenshot loop at 320/390/768/1024/1440 via Playwright against pnpm build + start; max three loops per direction.
8. Checks script in .local/design-exploration/tools/directions-check.cjs: per direction and width - horizontal overflow, buttons on one line, h1 inside its box, display weight, body characters per line, radius on img and buttons, solid buttons per viewport, bare ground and saturated pixels below the fold; focus-ring screenshot after Tab; one pass with reducedMotion reduce.
9. e2e spec tests/e2e/directions.spec.ts: each route has one h1, no horizontal overflow at 320 and 390, header action on one line.
10. Gates: pnpm check, pnpm build, e2e. A-007 evidence into .local/evidence/2026-09-23-first-fold-directions/.
11. Independent review: visual-reviewer + code-reviewer in parallel (09-REVIEW), max three rounds.
12. Report: local HTML side by side in .local/design-exploration/2026-09-23-first-fold-directions/, screenshots inlined, §4.8 numbers, one recommendation, one question to the operator (GE-003.04).
13. File anything odd as tasks; final summary; doc-lifecycle skill; merge task branch into phase/01.1-direction; push.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
2026-09-23: use the design-directions skill (.claude/skills/design-directions/SKILL.md) for this task. Its 'In this repo' section maps its steps onto 02 §4.6 and DS-006. When planning, say where the three directions live in the tree and when they are removed, so direction routes do not ride along to release.

Plan review round 1 (plan-reviewer): FINDINGS, 2 Material + 5 Minor + 3 Notes. All folded into the plan: checks for AC3 widened (320 screenshots, one-line buttons, h1 box, focus ring, reduced-motion pass, motion inside no-preference), guard verified with CONTEXT=production, GE-005.01 removal AC added, new checks script named, solid button per direction stated, DS-004/DS-007 values stated, labelling via demo bar. Notes: bare Unsplash URLs, no Unsplash+, font-synthesis none to expose a missing italic, demo bar is a Server Component.

Code review round 1 (code-reviewer, 68fb5a5..8619c3d): FINDINGS. Material: DS-008 sizes missed the cover factor (grove photo in image-led cluster ~0.56-0.78 of needed pixels) - fixed with coverFactor() in c4cf5f5. Minor: e2e button check could not fail - fixed, now checks every visible button for spill and viewport bounds. Notes: dynamic='error' added to the layout (guard runs at build only); a11y shell defects (header inside main, inert menu) shared by all three, owned by GE-004.05; AGENTS.md block harmless.
<!-- SECTION:NOTES:END -->
