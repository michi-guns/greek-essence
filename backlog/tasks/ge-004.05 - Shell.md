---
id: GE-004.05
title: Shell
status: To Do
assignee: []
created_date: '2026-09-21 19:14'
updated_date: '2026-09-23 19:53'
labels:
  - 'lane:product'
  - 'phase:P1.2'
milestone: m-1
dependencies: []
parent_task_id: GE-004
type: feature
ordinal: 31000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Planning ref: T-01.9 (docs/v1/04-ROADMAP.md)
est: 90/180/360 min (min/avg/max)
Phase P1.2 - Product lane

Shell: header + nav (desktop and mobile), footer, root layout, metadata defaults, skip-link, landmarks
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 The header follows the DS-006 header row: over a full-bleed Hero it sits on the photograph with an outline action from md; once the hero has passed, or on a page without a Hero, it takes the ivory ground and the action is Button variant=link in the nav label style; below md it is menu and wordmark only, with no pinned bottom bar - including after client-side navigation
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
2026-09-23 from GE-003.02 (02-DESIGN-SYSTEM DS-006): the root layout owns the motion script. (1) inline <head> script adds class js to <html> before first paint; (2) one IntersectionObserver via next/script sets data-inview on [data-reveal] (once) and data-past-hero on the header when the Hero leaves the viewport; (3) hidden state only inside @media (prefers-reduced-motion: no-preference) and .js; (4) set use.contextOptions.reducedMotion = 'reduce' in playwright.config.ts and do one normal-motion walk to check the reveal. No new client component (06 §6 stays at four). Header: sticky, unchanged; over a full-bleed hero it takes the ivory ground once data-past-hero is set.

2026-09-23 correction (GE-003.02 review round 2) — supersedes steps (1)-(2) of the note above: no inline head script. public/motion.js, loaded with <Script src="/motion.js" strategy="afterInteractive" />, adds class js to <html> as its first line (a failed script leaves content visible), runs the IntersectionObserver, and a MutationObserver on <main> re-binds [data-reveal] elements and the Hero after client-side navigation. The reduced-motion rule in globals.css also gets ::view-transition-group/old/new(*) { animation: none }. The normal-motion walk navigates Home -> a package page by link.

2026-09-23 second correction (GE-003.02 review round 3): motion.js adds js as its LAST line, after both observers are set up and after marking every [data-reveal] already in view as data-inview, so any exception leaves content visible. Do not load motion.js on /studio.
<!-- SECTION:NOTES:END -->
